mixin socialmedia(name, url, slug)
    li 
        a(href=`${url}` target="_blank", rel="nofollow noopener noreferrer")
            i(class=`bi bi-${name}`)
            span(class="footer__slug") #{slug}