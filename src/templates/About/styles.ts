import styled from 'styled-components'

export const Content = styled.section`
  text-align: center;
  display: flex;
  align-items: center;
  flex-direction: column;
  height: 100vh;
  max-width: var(--container);
  margin: auto;
`

export const HeadingDesign = styled.h1`
  font-size: var(--large);
  margin-bottom: var(--large);
`

export const textDesign = styled.div`
  p {
    font-size: var(--medium);
    color: var(--black);
  }
`

// export const aboutPhoto = styled.div``

export const aboutDescription = styled.div`
  p {
    font-size: var(--medium);
    color: var(--black);
  }
`

export const headingEconomist = styled.h1`
  font-size: var(--large);
  margin-bottom: var(--large);
`

export const textEconomist = styled.div`
  p {
    font-size: var(--medium);
    color: var(--black);
  }
`

export const briefEconomist = styled.div`
  p {
    font-size: var(--medium);
    color: var(--black);
  }
`

export const aboutExpertise = styled.h2`
  font-size: var(--large);
  margin-bottom: var(--large);
`

export const descriptionExpertise = styled.div`
  p {
    font-size: var(--medium);
    color: var(--black);
  }
`

export const aboutSharping = styled.h2`
  font-size: var(--large);
  margin-bottom: var(--large);
`

export const descriptionSharping = styled.div`
  p {
    font-size: var(--medium);
    color: var(--black);
  }
`
